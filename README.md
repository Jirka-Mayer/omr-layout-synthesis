OMR Layout synthesis experiments
================================

## After cloning

Set up the virtual environment:

```
python3 -m venv .venv
.venv/bin/python3 -m pip install -r requirements.txt
```

Download the datasets:

```
make download-muscima-pp
```
